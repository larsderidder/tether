name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install agent-sessions dependency from GitHub release tag
        run: pip install "agent-sessions @ git+https://github.com/Adminrealagi/agent-sessions.git@v0.2.1"

      - name: Install dependencies
        working-directory: agent
        run: pip install -e ".[dev]"

      - name: Run tests
        working-directory: agent
        run: pytest --cov=tether --cov-report=xml

      - name: Upload coverage
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v4
        with:
          files: agent/coverage.xml
          fail_ci_if_error: false

  build:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Python dependencies
        working-directory: agent
        run: pip install build

      - name: Install UI dependencies
        working-directory: ui
        run: npm ci

      - name: Build UI
        working-directory: ui
        run: npm run build

      - name: Copy UI to agent
        run: |
          rm -rf agent/tether/static_ui
          cp -r ui/dist agent/tether/static_ui

      - name: Install sidecar dependencies
        run: npm install --workspaces

      - name: Build sidecar bundles
        run: ./scripts/build-sidecars.sh

      - name: Sync README for PyPI
        run: |
          cp README.md agent/README.md
          sed -i 's|src="tether_compact_logo.png"|src="https://raw.githubusercontent.com/larsderidder/tether/main/tether_compact_logo.png"|' agent/README.md

      - name: Build package
        working-directory: agent
        run: python -m build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: agent/dist/
